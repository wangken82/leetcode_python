
Example 1:

Input: 123
Output: 321
Example 2:

Input: -123
Output: -321
Example 3:

Input: 120
Output: 21

def reverser(x):
    ispos=1 if x>0 else -1
    rev=0
    x=abs(x)
    while x:
        pop=x%10
        x=int(x/10)
        rev=rev*10+pop
    print(ispos*rev)
reverser(-189)
